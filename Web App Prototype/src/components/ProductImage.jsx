export default function ProductImage({ className = "w-20 h-20" }) {
  return (
    <div className={`${className} bg-sage-100 rounded-md flex items-center justify-center text-sm text-ink-muted`}>
      Image
    </div>
  )
}
